<launch>

  <!-- Start a mapping-friendly world setup with the front laser enabled. -->
  <include file="$(find me5413_world)/launch/world.launch">
    <arg name="spawn_mapping_obstacle" value="false" />
  </include>

  <!-- Manual driving is still the fastest way to get an initial map. -->
  <node name="teleop_twist_keyboard" pkg="teleop_twist_keyboard" type="teleop_twist_keyboard.py" output="screen" respawn="true"/>

  <!-- Launch GMapping -->
  <include file="$(find jackal_navigation)/launch/include/gmapping.launch" />

  <!-- Launch Rviz with the mapping view -->
  <node type="rviz" name="rviz" pkg="rviz" args="-d $(find me5413_world)/rviz/gmapping.rviz" output="log" respawn="true"/>

</launch>
