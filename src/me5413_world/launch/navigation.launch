<launch>

  <!-- Connect the robot to a keyboard teleop controller -->
  <node name="teleop_twist_keyboard" pkg="teleop_twist_keyboard" type="teleop_twist_keyboard.py" output="screen" respawn="true"/>

  <!-- Run the map server -->
  <!-- 提供地图，加载地图：floor:=1 或 floor:=2 -->
  <arg name="map_file" default="$(find me5413_world)/maps/liosam_pcd/my_map.yaml"/>
  <node name="map_server" pkg="map_server" type="map_server" args="$(arg map_file)" />
  
  <include file="$(find me5413_world)/launch/include/amcl.launch" />

  <!-- Mission Coordinator -->
  <node pkg="me5413_decision" type="task_coordinator_node.py" name="task_coordinator_node" output="screen" />

  <!-- Launch Move Base -->
  <!-- 负责规划和运动控制 - 切换至 TEB 局部规划器以支持动态避障 -->
  <include file="$(find me5413_world)/launch/include/move_base_teb.launch" />

  <!-- Launch Rviz with our settings -->
  <!-- 启动Rviz -->
  <node type="rviz" name="rviz" pkg="rviz" args="-d $(find me5413_world)/rviz/navigation.rviz" output="log" respawn="true"/>

  <node ns="me5413_world" pkg="me5413_world" type="goal_publisher_node" name="goal_publisher_node" output="screen" />

</launch>